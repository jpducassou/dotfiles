#!/usr/bin/env bash

# Wrapper function for Maven's mvn command.
mvn-color() {
	mvn $@ | grcat "$HOME/.grc/conf.maven"
}

if [[ "$TERM" == 'xterm' ]] && [ $(which grcat &> /dev/null) ]; then
	# Override the mvn command with the colorized one.
	alias mvn="mvn-color"
fi

