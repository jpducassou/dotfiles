#!/bin/bash

# ============================================================================
# This script replaces the shebang line so as to use "env"
# ============================================================================
find -name '*.pl' | xargs perl -pi -e "s{^#!/usr/bin/perl}{#!/usr/bin/env perl}"

# ============================================================================

